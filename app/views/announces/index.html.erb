<div class="container">
  <div class="row" id="announce-result-card">
    <% @announces.each do |announce| %>
    <ul>
      <div class= "result-card">
        <div class= "col-xs-12 col-md-6">
          <li> <%= link_to announce.title, announce %> </li>
          <li> <%= "#{announce.number_of_share} #{announce.kind.name}" %> </li>
        </div>
        <div class= "col-xs-12 col-md-6">
          <li> <%= t('announces.sell_price') %> <%= "#{announce.price} €" %> </li>
          <li> <%= link_to t('announces.display'), announce, class: "btn btn-primary" %> </li>
        </div>
      </div> <br>
    </ul>
    <% end %>
  </div>
</div>



