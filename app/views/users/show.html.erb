
  <%= render 'shared/profil_nav' %>
  <div class="container">
    <div class="row mt-0">
      <div class="col-md-8 col-md-offset-2">
        <div id="profil-cadre">
          <%= image_tag current_user.picture(:medium), class: "circle" %>

            <p><%= "Votre nom : #{current_user.last_name}" %></p>
            <%= "Votre prénom : #{current_user.first_name}" %>


            <%= link_to t('account.users.edit'), edit_user_registration_path, class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>









