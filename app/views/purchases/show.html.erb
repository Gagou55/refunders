
  <div class="top-account">
    <%= render 'shared/profil_nav'%>
    <div class="row">
      <h1>My purchase</h1>
      <div class="col-xs-3 text-center green-card col-md-offset-1">
        <%= image_tag @purchase.announce.company.logo, :class => 'img-rounded img-show' %> <br>
        <%= @purchase.announce.company.name %> <br>
        <%= "#{@purchase.announce.number_of_share} #{@purchase.announce.kind.name}" %> <br>
        <%= "#{@purchase.announce.price} €" %>
      </div>
    </div>
  </div>
</div>